setuptools<81
pip
jinja2
ramp-packer
RLTest @ git+https://github.com/RedisLabsModules/RLTest@abc312868
statistics
# Capped below redis-py 6: from 6 on, the client negotiates RESP3 by default, and RLTest's
# requirement is uncapped, so pip picks the newest. Under RESP3 TS.MRANGE ... GROUPBY replies
# with a map instead of rows and the suite's reply assertions fail (MOD-18913).
redis ~= 5.0.8
