# MOD-16382: disable cluster topology auto-refresh via the modern module config.
# Passed to tests through RLTest's Env(redisConfigFile=...). A redis config-file
# directive reaches RedisModule_LoadConfigs() (unlike a loadmodule-line arg), so the
# registered ts-topology-auto-refresh bool config is applied at module load time.
ts-topology-auto-refresh no
